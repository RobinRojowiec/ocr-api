dist: trusty
services:
  - docker

jobs:
  include:
    - stage: build docker image
      script:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USER" --password-stdin
        - docker build -t berndserker/ocr-api .
        - docker images
        - docker tag dev $DOCKER_USER/ocr-api:latest
        - docker push berndserker/ocr-api
